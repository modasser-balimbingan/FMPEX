{
  "name": "ardnet-modular-react-workspace",
  "private": true,
  "workspaces": [
    "ArdNet"
  ],
  "scripts": {
    "dev": "npm --prefix .\\ArdNet run dev",
    "build": "npm --prefix .\\ArdNet run build",
    "preview": "npm --prefix .\\ArdNet run preview"
  }
}
